package com.t1.tripfy.domain.dto.chat.cachelike;

import java.util.List;
import java.util.concurrent.ConcurrentHashMap;

import org.springframework.stereotype.Component;

import com.t1.tripfy.domain.dto.chat.ChatDetailDTO;

/**
 * <p>메시지 전송시마다 DB 삽입을 하는 로직을 피하기 위해 만든 클래스
 * <p>일단 wip -240531
 * */
@Component
public class ChatCache {
//	private final ConcurrentHashMap<CacheKey, List<ChatDetailDTO>> cache = new ConcurrentHashMap<>();
//	private final ConcurrentHashMap<Long, V>
	
}
